# PR｜任务ID与标题

## 范围和原因

关联任务/组件规格/ADR；本次做了什么、明确没有做什么。

## API 与设计影响

React/Vue是否同步；新增或变化props/events/slots/parts/tokens/exports；受控与默认行为；是否破坏兼容；是否需要changeset与迁移。

## 测试证据

| 命令/人工用例 | 环境/commit | 结果 | 日志/报告 |
|---|---|---|---|
| 待执行 | 待填 | not-run | 无 |

结果只能pass/fail/not-run/blocked。截图另外说明能证明的视觉状态，不代替交互/SSR/pack证据。

## 自查

- [ ] 无秘密、private依赖泄漏或框架重复运行时。
- [ ] 关键边界、清理、无障碍、移动、主题、类型通过。
- [ ] 文档、样例、导出、CSS依赖和changeset同步。
- [ ] 未运行项、风险和破坏性变更已明确。

## 风险与回退

如何撤回本次代码；已公开版本不得覆盖；未验证环境及后续任务。
