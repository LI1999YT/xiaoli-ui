# <真实包名>

> 此为未来公开包README模板。发布前替换占位、删除未实现功能与未验证兼容性，不直接把本段当宣传。

一句话用途。明确这是React或Vue的Web组件包，只需要对应框架，不包含另一框架运行时。

## 安装与样式

使用真实package名称、已发布版本与包管理器命令；列出经过测试的peer版本范围。给出完整可编译的基础组件和CSS导入示例，不能省略必需Provider或样式。

## 快速使用

至少一个可操作示例：输入值、点击动作、明确的事件处理。Vue用正确v-model，React用正确受控回调。完整源码应在消费者fixture中运行。

## 主题和自定义

链接已发布文档，示范Provider/theme、组件token、parts、局部主题。说明unstyled保留必要结构样式，portal如何继承配置，不宣传任意CSS自动跨portal。

## 按需与兼容

根入口/子路径/按需CSS、ESM-only边界、SSR集成和实测浏览器矩阵。区分测试通过与目标支持，移动指H5。

## 状态、问题和授权

稳定组件范围、实验模块、CHANGELOG、迁移指南、真实issue/security入口、贡献方式和许可证。没有的地址不编造；安全联系人未落实前不填写他人邮箱。
