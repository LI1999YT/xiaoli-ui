# Cxx｜组件英文名 · 中文名（新组件模板）

状态：待评审；优先级：待定；作者/评审：待填写。新增组件先评估是否与现有72项重复。

## 需求与边界

解决的具体场景、使用者、PC/移动差异、不包含项、采用/不采用其他组件的原因。

## 类型与 API

| 属性 | React / Vue 映射 | 类型 | 默认 | 必填/空值/受控规则 |
|---|---|---|---|---|
| 待填 | 待填 | 待填 | 待填 | 待填 |

事件列出参数、reason、调用时序、可取消性；命名模型、ref/expose返回类型明确。Promise执行器与观察事件分开。slots/render上下文、原生属性落点和ref组合规则明确。

## 状态与交互

状态图或状态表；鼠标/触屏/键盘；受控父不更新、数据刷新、异步竞态、取消、重置、卸载。没有的状态写不适用，不堆无意义props。

## 主题、部位与移动

公开data-part；token键/类型/默认/fallback；局部主题/portal/unstyled/RTL；长文本、320px、横屏、软键盘、安全区、低动效、手势替代。

## 无障碍与 SSR

role、可访问名称、label/error关系、焦点进入/移动/退出、live region、APG参考与人工验证。服务器输出、初次hydration、ID、DOM监听器清理与降级。

## 验收与产物

| caseId | 输入与操作 | 预期 | React证据 | Vue证据 |
|---|---|---|---|---|
| Cxx-EDGE-01 | 待填 | 待填 | 未运行 | 未运行 |

根/子路径/CSS导出、依赖图、双框架stories、类型消费用例、性能预算、changeset、迁移影响。
