# 发布检查提示词（默认只验证，不发布）

```text
请准备本项目发布候选，默认只检查与打包，不执行公开npm操作。
先读 docs/14、18、19、20、22、25 与 tracking/RELEASE-CHECKLIST.md；以当前官方文档再次核验Node/npm、可信发布者allowed actions、runner支持与provenance条件，记录核验日期。

检查名称、scope所有权、真实仓库与许可证、四包版本同步、changeset、peer和内部依赖、源码与已批准commit一致、无秘密文件与不明来源资源。
从干净环境按冻结lockfile构建；生成并审核同一份tarball与release manifest。检查exports/type/CSS/相对路径存在，private/workspace不泄漏，React/Vue不重复打包。
在仓库外consumer分别用npm与pnpm安装tarball，执行双框架构建与关键交互、SSR、按需CSS和体积检查。不要依赖workspace alias使失败消失。

核对GitHub environment人工批准、id-token:write与每包可信发布者；固定已验证Actions SHA，不猜版本。GitHub人工批准+直接npm publish方案必须在npm显式允许直接publish；stage-only另走正式设计流程。

输出：候选版本与tag、包清单/依赖顺序、commit、tarball SHA256/registry integrity核验计划、测试证据、已知风险、待授权事项及半成功恢复方案。
发现阻断立即停止公开操作，但继续可完成检查。未经所有者明确授权，不publish、unpublish、deprecate或改tag；不得把dry-run当远端成功。

如后续获得明确发布授权，只发布已审核的精确tarball：tokens→theme→react/vue。遇到半成功核对已存在版本与integrity，仅补发同一份缺失产物，不改同版本内容。发布后从真实registry安装明确版本验证，并保存report。
```
