# AGENTS

完整规格位于 [`react-vue-component-library-docs/`](./react-vue-component-library-docs/)。实现阶段以该目录的 `AGENTS.md`、`docs/`、`components/` 与 `tracking/` 为准。

当前代码阶段：M0 底座 + M1 十项试点。不要一次性生成全部 72 项，不要擅自 `npm publish`。
